version: '3.8'
services:
    nginx:
        image: nginx:latest
        ports:
            - '2910:80'
        volumes:
            - ./dist:/usr/share/nginx/html
            - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
        restart: unless-stopped
